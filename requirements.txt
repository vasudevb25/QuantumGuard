# QuantumGuard - all phases
#
#   pip install -r requirements.txt
#
# Everything here is a pure-Python or wheel-only install: no compile
# step, no CUDA, and no liboqs build required. (The eBPF capture layer
# itself is C, built separately via `make -C ebpf` - see README.md.)

# --- Phase 1/2/3: capture storage and provenance graph ---
sqlalchemy>=2.0
psycopg2-binary>=2.9
networkx>=3.0
matplotlib>=3.7

# --- Phase 4: features + GraphSAGE (pure NumPy implementation) ---
numpy>=1.24

# --- Phase 5: classical crypto, AES-GCM key wrapping, scrypt ---
cryptography>=42.0

# --- Phase 5: post-quantum signatures (ML-DSA / FIPS 204) ---
# Pure Python, no native build. Signing takes a few milliseconds.
dilithium-py>=1.0

# Optional faster ML-DSA backend. crypto/integrity.py's MLDSA class picks
# it up automatically if it imports, and falls back to dilithium-py
# otherwise. Needs liboqs compiled on the host, so it is left commented
# out by default.
# liboqs-python>=0.10

# Optional: nicer graph/visualize.py layout via Graphviz's `dot`.
# graph/visualize.py falls back to a spring layout if this isn't
# installed, so it is not required.
# pygraphviz>=1.12
